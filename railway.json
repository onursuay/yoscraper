{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "python setup_credentials.py && python dashboard.py",
    "restartPolicyType": "ON_FAILURE"
  }
}
